# Threads Agent 🤖

Автономный агент для публикации постов в Threads через Playwright.

## Структура
- `post.js` — публикует пост в Threads
- `check-session.js` — проверяет живость сессии
- `cookies.json` — куки сессии (в .gitignore, не коммитить!)

## Использование
```bash
# Проверить сессию
node check-session.js

# Опубликовать пост
node post.js "Текст поста"
```

## Обновление куки
Если сессия протухла:
1. Открой threads.net в Chrome
2. Cookie-Editor → Export → JSON
3. Замени содержимое cookies.json
