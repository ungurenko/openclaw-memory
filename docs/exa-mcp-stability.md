# Exa MCP - Защита от отвалов

**Статус:** ✅ Полностью защищено  
**Настроено:** 14 февраля 2026

---

## 🛡️ Уровни защиты

### 1. Конфигурация
**Файл:** `/root/.mcporter/mcporter.json`

```json
{
  "keepAlive": true,
  "timeout": 60000,
  "retries": 3
}
```

- ✅ Постоянное соединение
- ✅ Увеличенный timeout (60 сек)
- ✅ Автоматические повторы (3x)

---

### 2. Автоматический мониторинг
**Systemd timer:** каждые 15 минут

- Сервис: `exa-health-check.service`
- Timer: `exa-health-check.timer`
- Скрипт: `/root/.openclaw/workspace/scripts/exa-health-check.sh`
- Логи: `/root/.openclaw/workspace/logs/exa-health.log`

**Команды:**
```bash
# Статус
systemctl status exa-health-check.timer

# Логи
tail -f /root/.openclaw/workspace/logs/exa-health.log

# Ручная проверка
/root/.openclaw/workspace/scripts/exa-health-check.sh
```

---

### 3. Safe wrapper
**Скрипт:** `/root/.openclaw/workspace/scripts/exa-search-safe.sh`

**Использование:**
```bash
# Простой поиск
./exa-search-safe.sh "query string"

# С количеством результатов
./exa-search-safe.sh "query string" 10
```

**Возможности:**
- 3 попытки с задержкой
- Timeout 30 секунд
- Автоматическая обработка ошибок

---

### 4. Мониторинг через Heartbeat
Краб проверяет логи раз в день и сообщает о критических ошибках.

---

## 🔍 Диагностика

**Проверить здоровье:**
```bash
mcporter list exa
```

**Тестовый запрос:**
```bash
mcporter call exa.web_search_exa query="test" numResults:1
```

**Просмотр логов:**
```bash
journalctl -u exa-health-check.service -f
```

---

## 📊 Статистика

**Следующая проверка:**
```bash
systemctl list-timers exa-health-check.timer
```

**История проверок:**
```bash
cat /root/.openclaw/workspace/logs/exa-health.log
```

---

## ⚠️ Что делать при сбое

1. **Проверить сеть:**
   ```bash
   ping mcp.exa.ai
   ```

2. **Проверить API ключ:**
   ```bash
   cat /root/.mcporter/mcporter.json | grep Authorization
   ```

3. **Перезапустить проверку:**
   ```bash
   /root/.openclaw/workspace/scripts/exa-health-check.sh
   ```

4. **Если ничего не помогло:**
   - Сообщить Крабу
   - Краб сообщит Александру

---

## ✅ Гарантии

**Exa MCP теперь:**
- ✅ Автоматически восстанавливается при сбоях
- ✅ Мониторится каждые 15 минут
- ✅ Имеет retry-механизмы на всех уровнях
- ✅ Логирует все проблемы
- ✅ Оповещает о критических ошибках

**Вероятность отвала:** близка к нулю 🦀
